package com.kvad.totalizator.account.registration

enum class RegisterState {
    USERNAME_ERROR,
    EMAIL_ERROR,
    PASSWORD_LENGTH_ERROR,
    BIRTHDAY_ERROR,
    NETWORK_ERROR,
    WITHOUT_ERROR
}
