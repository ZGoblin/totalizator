package com.kvad.totalizator.registration

enum class RegisterState {
    LOGIN_LENGTH_ERROR,
    USERNAME_ERROR,
    PASSWORD_LENGTH_ERROR,
    BIRTHDAY_ERROR,
    NETWORK_ERROR,
    WITHOUT_ERROR
}
